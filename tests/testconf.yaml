mediator:
  baseUri: "http://localhost:9027"
  routes:  
    trackSearch:
      path: "/tracks/search"
      args: ["pattern"]

    trackShow:
      path: "/tracks/%s"
      args: ["source"]

    collectionSearch:
      path: "/collections/search"
      args: ["pattern"]
      
